---
description: "Run Bugfix-Flow for a Jira issue or bug description"
argument-hint: "<Jira issue key, URL, or bug description>"
---

Use the `bugfix-flow` skill with these arguments:

```text
$ARGUMENTS
```

If no Jira issue key, URL, or bug description is provided, ask the user for one concise clarification.

Treat this command as equivalent to:

```text
使用 bugfix-flow 处理 $ARGUMENTS
```
