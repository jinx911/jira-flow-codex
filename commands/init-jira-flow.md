---
description: "Initialize Jira-Flow Codex for the current project"
argument-hint: "[project path]"
---

Use the `init-jira-flow` skill with these arguments:

```text
$ARGUMENTS
```

If no project path is provided, initialize the current workspace.

Treat this command as equivalent to:

```text
使用 init-jira-flow 初始化 $ARGUMENTS
```

Follow the Codex-native setup workflow in `skills/init-jira-flow/SKILL.md`.
