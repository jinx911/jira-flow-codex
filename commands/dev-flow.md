---
description: "Run Dev-Flow Codex for a Jira issue key or a free-form requirement"
---

Use the `dev-flow` skill with these arguments:

```text
$ARGUMENTS
```

If no Jira issue key, Jira URL, or requirement text is provided, ask the user for one concise clarification.

Treat this command as equivalent to:

```text
使用 dev-flow 处理 $ARGUMENTS
```

Follow the workflow in `skills/dev-flow/SKILL.md`.
