---
description: "Initialize Dev-Flow Codex for the current project"
argument-hint: "[project path]"
---

Use the `init-dev-flow` skill with these arguments:

```text
$ARGUMENTS
```

If no project path is provided, initialize the current workspace.

Treat this command as equivalent to:

```text
使用 init-dev-flow 初始化 $ARGUMENTS
```

Follow the setup workflow in `skills/init-dev-flow/SKILL.md`.
